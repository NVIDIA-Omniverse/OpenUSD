#usda 1.0
(
    doc = "Session layer for ticket 418: caustics off with straight transparent shadow approximation disabled."
)

over "RenderSettings"
{
    bool ty:enableCaustics = false
    bool ty:approxTransparentShadows = false
}
