#usda 1.0
(
    doc = "Session layer for ticket 418: caustics off with straight transparent shadow approximation enabled."
)

over "RenderSettings"
{
    bool ty:enableCaustics = false
    bool ty:approxTransparentShadows = true
}
