#usda 1.0
(
    defaultPrim = "World"
    metersPerUnit = 1.0
    renderSettingsPrimPath = "/RenderSettings"
    subLayers = [
        @openpbr_sss_native_adobe_compare.usda@
    ]
    upAxis = "Y"
)

over "RenderSettings"
{
    bool ty:useAdobeOpenPBR = true
}
