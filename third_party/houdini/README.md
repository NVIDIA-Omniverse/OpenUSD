The legacy USD Houdini plugins have been deprecated in favor of Houdini's
native Solaris suite (also known as LOPs).

The `typhoon/` directory is separate from those legacy plugins. It packages the
Typhoon Hydra render delegate against the OpenUSD and ABI-sensitive libraries
provided by Houdini 22. See `typhoon/BUILD.md` for the supported workflow.
